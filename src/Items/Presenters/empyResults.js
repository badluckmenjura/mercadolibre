import React from 'react';
import PropTypes from 'prop-types'

const EmpyResults = ({
  }) => (

  < div className = "empy">
    <p>La búsqueda no arrojo resultados</p>
  </div>
)

EmpyResults.propTypes = {
}

export default EmpyResults;