import React from 'react';
import PropTypes from 'prop-types'

const EmpySearh = ({
  }) => (

  < div className = "empy">
    <p>Por favor realice una búsqueda desde el buscador</p>
  </div>
)

EmpySearh.propTypes = {
}

export default EmpySearh;